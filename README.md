# GOAL93612 Backend

This is the backend for the GOAL93612 project, a clothing store inventory system.

## Getting Started

1. Clone the repository.
2. Set up a virtual environment and activate it.
3. Install dependencies: `pip install -r requirements.txt`
4. Run the server: `uvicorn main:app --reload`

Ensure that the database URL is set correctly in `.env.example`.